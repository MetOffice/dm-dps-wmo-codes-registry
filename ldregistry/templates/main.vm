#if($uri)#set($pageTitle="WMO Codes Registry: $uri")#end
#parse("header.vm")

#if(!$uri.isEmpty() || $entity  || ${_browse})
  #parse("page-deref.vm")
#else
  #parse("page-category.vm")
#end

#parse("footer.vm")
